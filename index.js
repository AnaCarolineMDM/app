// arrays, objetos

let meta = {
    value: 'ler um livro por mês',
    checked: false,
    isChecked: (infor) => {
        console.log(info)
    }
}

meta.log(meta.volue)

// function // arrow function

const criarMeta = () => {}

function criarMeta() {}